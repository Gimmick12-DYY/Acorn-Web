# Environment variables placeholder
# NEXT_PUBLIC_* variables are exposed to the browser.
# Add secrets in the Vercel dashboard or a .env.local file (not committed).
NEXT_PUBLIC_SITE_NAME=Acorn

# News updates are stored locally in apps/web/data/news.json — no database required.
